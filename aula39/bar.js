/* function move() {
    let elem = document.getElementById();
    let widht = 0;

    let id = setInterval(frame, 1000);

    function frame() {
        if (widht >= 100){
            clearInterval(id);
        }
        else {
            widht++;
            elem.style.width = widht + "%";
            elem.innerHTML = widht + "%";
        }
    }
} */
